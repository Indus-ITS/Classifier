"""Library + utility scripts behind the main classifier.py.

`classifier_lib` exposes the bucket scoring / regex helpers used by the main
`classifier.py` entry point at the project root.

`harvest_labelled.py` and `evaluate_corpus.py` are evaluation tools — run them
from the project root when you want to regenerate the labelled corpus or
re-measure classifier accuracy after editing config.py.
"""
