"""Shared pytest fixtures.

Currently empty - all unit tests work directly with helpers/classifier_lib
functions and don't need filesystem fixtures. Kept as a placeholder so
tests/ remains a discoverable package and future fixtures have a home.
"""
